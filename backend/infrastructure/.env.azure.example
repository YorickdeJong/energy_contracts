# =============================================================================
# Azure Deployment Configuration
# =============================================================================
# Copy this file to .env.azure and fill in your Azure-specific values
# This file contains deployment credentials and configuration
# NEVER commit .env.azure to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Azure Subscription & Authentication
# -----------------------------------------------------------------------------

# Your Azure Subscription ID
# Find it: az account show --query id --output tsv
# Or in Azure Portal: Home > Subscriptions
AZURE_SUBSCRIPTION_ID=

# Azure Tenant ID (for multi-tenant scenarios)
# Find it: az account show --query tenantId --output tsv
AZURE_TENANT_ID=

# -----------------------------------------------------------------------------
# Resource Group Configuration
# -----------------------------------------------------------------------------

# Resource group name (will be created if it doesn't exist)
AZURE_RESOURCE_GROUP=energy-contracts-rg

# Azure region/location for resources
# Common options: westeurope, eastus, westus2, northeurope
# See all regions: az account list-locations --query "[].name" --output tsv
AZURE_LOCATION=westeurope

# -----------------------------------------------------------------------------
# Deployment Parameters
# -----------------------------------------------------------------------------

# Environment name (dev, staging, prod)
# This affects resource naming and some default settings
ENVIRONMENT=dev

# Project name (used as prefix for all resource names)
PROJECT_NAME=energy-contracts

# Parameters file to use (create from parameters.example.json)
# For production: parameters.prod.json
# For staging: parameters.staging.json
PARAMETERS_FILE=parameters.dev.json

# -----------------------------------------------------------------------------
# PostgreSQL Configuration
# -----------------------------------------------------------------------------

# PostgreSQL administrator username
POSTGRES_ADMIN_USERNAME=pgadmin

# PostgreSQL administrator password
# REQUIREMENTS:
# - At least 8 characters
# - Must contain characters from three of: uppercase, lowercase, numbers, special chars
# - Cannot contain username
# GENERATE SECURE PASSWORD:
#   openssl rand -base64 32
POSTGRES_ADMIN_PASSWORD=

# -----------------------------------------------------------------------------
# Django Configuration
# -----------------------------------------------------------------------------

# Django SECRET_KEY (used for cryptographic signing)
# REQUIREMENTS:
# - At least 50 characters
# - Should be random and unpredictable
# GENERATE SECRET KEY:
#   python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
DJANGO_SECRET_KEY=

# -----------------------------------------------------------------------------
# Optional: Service Principal for CI/CD
# -----------------------------------------------------------------------------
# If deploying via GitHub Actions or Azure DevOps, create a service principal:
# az ad sp create-for-rbac --name "energy-contracts-deploy" --role contributor \
#   --scopes /subscriptions/{subscription-id}/resourceGroups/{resource-group} \
#   --sdk-auth

# Service Principal Application (Client) ID
AZURE_CLIENT_ID=

# Service Principal Secret
AZURE_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# Optional: Custom Domain & SSL
# -----------------------------------------------------------------------------

# Custom domain for the application (optional)
# CUSTOM_DOMAIN=api.yourdomain.com

# SSL certificate thumbprint (if using custom domain)
# SSL_CERT_THUMBPRINT=

# -----------------------------------------------------------------------------
# Cost Management Alerts (Optional)
# -----------------------------------------------------------------------------

# Email for budget alerts
BUDGET_ALERT_EMAIL=

# Monthly budget limit in USD
MONTHLY_BUDGET=50

# -----------------------------------------------------------------------------
# Notes
# -----------------------------------------------------------------------------
# After filling in this file:
# 1. Copy to .env.azure: cp .env.azure.example .env.azure
# 2. Fill in all required values (those without defaults)
# 3. Add .env.azure to .gitignore (should already be there)
# 4. Run deployment: ./deploy.sh
#
# To get your Azure credentials:
# 1. Install Azure CLI: https://docs.microsoft.com/cli/azure/install-azure-cli
# 2. Login: az login
# 3. List subscriptions: az account list --output table
# 4. Set default subscription: az account set --subscription "<subscription-id>"
# 5. Verify: az account show
